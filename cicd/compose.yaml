services:
  petclinic:
    image: $REGISTRY_USER/$APP_NAME-$BRANCH_NAME:$GIT_COMMIT
    ports:
      - "$EXPOSE_PORT:8080"
  mysql:
    image: mysql:9.1
    expose:
      - 3306
    volumes:
      - ../conf.d:/etc/mysql/conf.d:ro
