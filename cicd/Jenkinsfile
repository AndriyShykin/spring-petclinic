pipeline {
  agent none
  environment {
    APP_NAME = 'spring-petclinic'
  }
  stages {
    stage('Build') {
      options {
        timeout(time: 300, unit: 'SECONDS')
      }
      when {
        allOf {
          anyOf {
            changeset "src/main/**"
            triggeredBy 'UserIdCause'
          }
          expression {
            env.BRANCH_NAME in ['main', 'test']
          }
        }
      }
      agent { dockerContainer { image 'maven:3.9.9-eclipse-temurin-24-alpine' } }
      steps {
        sh 'ls -la'
        sh 'mvn'

      }
    }
  }
}
